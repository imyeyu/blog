export type Software = {
	logo?: string;         // 图标地址
	url?: string;          // 地址
	version?: string;      // 版本
	runtime?: string;      // 应用平台
	size?: number;         // 文件大小（字节）
	format?: string;       // 文件格式
	password?: string;     // 解压密码
	dlURL?: string;        // 下载地址
}